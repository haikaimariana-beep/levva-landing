# Todos vazios neste build (front-only). Preencher ao ligar backend/analytics.
VITE_GTM_ID=
VITE_GA4_ID=

# Supabase — Project Settings > API no painel do seu projeto.
# VITE_SUPABASE_ANON_KEY é a chave pública ("anon"), segura pra expor no front
# DESDE QUE a tabela "leads" tenha RLS habilitado com policy só de INSERT (ver instruções).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
